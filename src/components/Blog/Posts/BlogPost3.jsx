import BlogPostContent from "./BlogPostContent.jsx";

/** "Barcodes" article — content lives in the locales (blog.blogs.barcodes). */
export default function BlogPost3() {
  return <BlogPostContent postKey="barcodes" image="/regal.jpg" />;
}
