import BlogPostContent from "./BlogPostContent.jsx";

/** "Technical debt" article — content lives in the locales (blog.blogs.debt). */
export default function BlogPost1() {
  return <BlogPostContent postKey="debt" image="/schuld.png" />;
}
